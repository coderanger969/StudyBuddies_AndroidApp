<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"  android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.sanskrutinaik.study_buddies.Schedule">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/scheduleAMeet"
        android:id="@+id/textView"
        android:textStyle="bold"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Reserve A Room"
        android:id="@+id/reserve"
        android:layout_gravity="bottom|center"
        android:onClick="gotoWeb"
        android:layout_marginTop="39dp"
        android:layout_below="@+id/textView"
        android:layout_centerHorizontal="true" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Meeting Date"
        android:id="@+id/meetingDate"
        android:onClick="datePicker"
        android:layout_above="@+id/meetingTime"
        android:layout_alignRight="@+id/meetingTime"
        android:layout_alignEnd="@+id/meetingTime"
        android:layout_marginBottom="76dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="Meeting Date"
        android:id="@+id/setDate"
        android:layout_alignBottom="@+id/meetingDate"
        android:layout_alignLeft="@+id/setTime"
        android:layout_alignStart="@+id/setTime" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Meeting Time"
        android:id="@+id/meetingTime"
        android:onClick="timePicker"
        android:layout_gravity="bottom|center"
        android:layout_above="@+id/scheduleButton"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_marginBottom="80dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/setTime"
        android:hint="Meeting Time"
        android:layout_alignBottom="@+id/meetingTime"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />


    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Schedule"
        android:id="@+id/scheduleButton"
        android:layout_gravity="bottom|center"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
